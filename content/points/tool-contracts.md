# 工具契约：幂等、超时、重试、可观测

## 目标
把工具调用变成可控的工程系统。

## 1) 幂等
同一请求重复调用，不应该产生重复副作用。

## 2) 超时
每个工具必须有 timeout。

- 超时后返回明确错误
- 让 Harness 做降级

## 3) 重试
不要盲目重试：

- 区分可重试错误（网络抖动）与不可重试错误（参数非法）
- 设重试上限

## 4) 可观测
每次调用记录：

- 输入摘要
- 输出摘要
- 耗时
- token/cost

