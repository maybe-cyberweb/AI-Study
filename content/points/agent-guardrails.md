# 安全护栏：政策红线、PII 与越权防护

## 先说结论
系统提示词不是安全。安全来自：

- 权限
- 数据隔离
- 审计
- 最小化暴露

## 1) 政策红线
把红线固化为：

- 规则库（不可由模型修改）
- 审计日志
- 拒答模板

## 2) PII 与敏感数据
- 最小化返回字段
- 脱敏
- 按角色授权

## 3) 越权防护
工具调用必须带：

- caller 身份
- scope（组织/角色/用户）

后端再做校验，而不是相信模型。

