# 死循环与烧钱：工具调用的预算与熔断

## 常见现象
模型连续多次调用同一个工具失败，然后继续调用，导致 token 账单爆炸。

## 1) 预算策略
- 单工具最大次数（例如 3）
- 全局最大工具次数（例如 8）
- token 上限

## 2) 熔断与降级
- 达到阈值立刻终止循环
- 返回“默认方案 + 人工提示”

## 3) 把错误分类
- 参数错误 -> 让模型修参数
- 权限错误 -> 直接拒绝
- 系统错误 -> 走备用路由

